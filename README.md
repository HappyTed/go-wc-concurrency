# go-wc 🧮

Утилита `wc` (word count) на Go с использованием пула воркеров. 
Подсчитывает количество строк, слов и байт в файлах или стандартном вводе, эффективно распределяя работу между горутинами.

## 🚀 Особенности

- Поддержка подсчёта:
  - строк (`-l`)
  - слов (`-w`)
  - байт (`-b`)
- Пул воркеров для обработки нескольких файлов
- Совместима с Unix-пайпами (`cat file.txt | go-wc`)

## 📦 Установка

```bash
git clone https://github.com/HappyTed/go-wc-concurrency.git
make build
```

## 🔧 Использование
```bash
./go-wc -w file1.txt file2.txt
./go-wc -l *.go
cat main.go | ./go-wc -b
```

### ⚙️ Опции

| Флаг | Описание                                           |
| ---- | -------------------------------------------------- |
| -w   | Подсчитать слова                                   |
| -l   | Подсчитать строки                                  |
| -c   | Подсчитать байты                                   |
| -n   | Количество воркеров (по умолчанию: число ядер CPU) |
