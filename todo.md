
Структура: 
``` 
go-wc/
├── README.md
├── go.mod
├── LICENSE
├── Makefile
├── main.go
├── internal/
│   └── workerpool/
│       └── pool.go
└── pkg/
    └── wc/
        ├── wc.go
        └── wc_test.go
```

**Пояснение:**
- cmd/wc — точка входа CLI-утилиты
- pkg/wc — логика подсчёта строк, слов, байт и т.д.
- internal/workerpool — реализация пула воркеров
- wc_test.go — юнит-тесты
- README.md — описание
- LICENSE — MIT

---

🧾 README.md (пример)
```md
# go-wc 🧮

Утилита `wc` (word count) на Go с использованием пула воркеров.  
Подсчитывает количество строк, слов и байт в файлах или стандартном вводе, эффективно распределяя работу между потоками.

## 🚀 Особенности

- Поддержка подсчёта:
  - строк (`-l`)
  - слов (`-w`)
  - байт (`-c`)
- Пул воркеров для обработки нескольких файлов
- Без внешних зависимостей
- Совместима с Unix-пайпами (`cat file.txt | go-wc`)

## 📦 Установка

\`\`\`bash
git clone https://github.com/<username>/go-wc.git
cd go-wc/cmd/wc
go build -o go-wc
```

---

🔧 Использование
```bash
./go-wc -w file1.txt file2.txt
./go-wc -l *.go
cat main.go | ./go-wc -c
```

так же работа с Makefile

---

⚙️ Опции
Флаг	Описание
-w	Подсчитать слова
-l	Подсчитать строки
-c	Подсчитать байты
-n	Количество воркеров (по умолчанию: число ядер CPU)

📜 Лицензия

MIT


---

## 🛡️ Лицензия (MIT)

Создай файл `LICENSE` с таким содержимым:

```text
MIT License

Copyright (c) 2025 <Your Name>

Permission is hereby granted, free of charge, to any person obtaining a copy...

---

🌟 Дополнительно

- Добавь GitHub Topics: golang, cli, concurrency, workerpool, wc.

- Добавь CI (Go vet + Go test).

- Можно прикрепить benchmark’и (go test -bench .) — это покажет производительность параллельного решения.